
shape curve(number shrink, number turn)

startshape curve(0.98, 3) [sat 1 b 1]
//startshape curve() {sat 1 b 1}

shape curve
{
  SQUARE []
  curve(=) [[y 0.5 r turn s shrink y 0.5]]
}

shape curve2(number shrink, number turn)
//shape curve2(number shrink, adjustment turn)
{
  curve(=) []
}
